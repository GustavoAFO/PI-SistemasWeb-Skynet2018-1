{
  "rules": {
    ".read": "auth.uid != null && root.child('users/' + auth.uid).exists()",
    ".write": true,
    "alertas": {
      "$groupid": {
        ".indexOn": "exibido"
      }
    },
    "Nodes":{
     ".indexOn": "nodeMCU"
    }
  }
}
